@if (TempData["Message"] != null)
{
    <div class="alert alert-@TempData["MessageType"] alert-dismissible fade show mt-2">
        @TempData["Message"]
        <button class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}